pub fn process(input: &str) -> u16 {
    let mut sum: u16 = 0;
    sum
}

#[cfg(test)]
mod tests{
    use super::*;

    #[test]
    fn test_part1(){
        let result = process("");
        assert_eq!(result, 0);
    }
}

